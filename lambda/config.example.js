// Copy this file to lambda/config.js in the Alexa developer console (Code tab)
// and replace the two REPLACE_ME values with your skill's Alexa Client Id and
// Alexa Client Secret (Build tab > Tools > Permissions > Alexa Skill Messaging).
// Never commit config.js. It is listed in .gitignore.
module.exports = {
  skillClientId: 'REPLACE_ME',
  skillClientSecret: 'REPLACE_ME',
  // Leave blank to use the default Far East endpoint (https://api.fe.amazonalexa.com).
  dataStoreApiEndpoint: '',
};
